# Force Python-only build for backend
# Prevents Nixpacks from detecting frontend/package.json

providers = ["python"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python run_backend.py"
